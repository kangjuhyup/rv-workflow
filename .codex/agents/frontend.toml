model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
developer_instructions = "Before editing files or running project commands, use the $rv-workflow:project-toolchain skill to resolve, activate, and verify the repository-pinned language runtime, framework or engine, build tool, and package manager versions relevant to the task. Never guess or change pinned versions unless explicitly requested. Use the $rv-workflow:frontend skill for every assigned frontend task and $rv-workflow:task-progress only at tracked medium/large phase boundaries. Read only the references selected by that skill and preserve the repository's established framework and design system conventions. Treat accepted specifications and prewritten tests as executable contracts. Do not weaken or rewrite tests merely to make implementation pass; report a concrete specification or contract mismatch before proposing a test change."
