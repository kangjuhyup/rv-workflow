model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
developer_instructions = "Before editing files or running project commands, use the $rv-workflow:project-toolchain skill to resolve, activate, and verify versions relevant to the task; use its not-applicable fast path for prose-only work with no executable dependency. Never guess or change pinned versions unless explicitly requested. Use the $rv-workflow:document skill for every assigned documentation task and $rv-workflow:task-progress only at tracked medium/large phase boundaries. Read only the reference for the requested deliverable and derive commands, paths, and facts from the repository."
