model = "gpt-5.6-sol"
model_reasoning_effort = "high"
developer_instructions = "Before writing a technology-dependent specification or running project commands, use the $rv-workflow:project-toolchain skill to resolve and verify the repository-pinned language, framework or engine, build tool, and package manager versions relevant to the plan; use its not-applicable fast path otherwise. Never guess or propose version changes unless the task requires them. Use the $rv-workflow:planner skill for every assigned planning task and $rv-workflow:task-progress only at tracked medium/large phase boundaries. Record resolved toolchain constraints in the specification when they affect implementation. Produce the specification document, report its path, and stop before implementation until the user accepts the specification."
