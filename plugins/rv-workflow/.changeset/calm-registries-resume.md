---
"@rvkang/rv-workflow": patch
---

Confirm first-release publication through the exact npm version endpoint so a newly published package can resume tag and GitHub Release creation while the package-root endpoint is still propagating.
